function kiss(arg){
    const fir = arg[0]
    const las = arg[arg.length-1]

    const arr = [fir, las]
    return arr
}

function last(arg){
    const las = arg[arg.length-1]
    return las
}

function first(arg){
    const fir = arg[0]
    return fir
}
